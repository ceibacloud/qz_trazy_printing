<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Add Print Receipt button to POS Order form view -->
    <record id="view_pos_order_form_print_receipt" model="ir.ui.view">
        <field name="name">pos.order.form.print.receipt</field>
        <field name="model">pos.order</field>
        <field name="inherit_id" ref="point_of_sale.view_pos_pos_form"/>
        <field name="arch" type="xml">
            <!-- Add Print Receipt button in the header -->
            <xpath expr="//header" position="inside">
                <button name="action_print_receipt" 
                        type="object" 
                        string="Print Receipt" 
                        class="btn-primary"
                        invisible="state not in ['paid', 'done', 'invoiced']"/>
            </xpath>
        </field>
    </record>
</odoo>
